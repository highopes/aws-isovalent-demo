apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: cilium-eks-demo
  region: ap-southeast-1
  version: "1.30"

iam:
  withOIDC: true

addonsConfig:
  disableDefaultAddons: true
addons:
- name: coredns

