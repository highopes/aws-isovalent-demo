apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: k8s-demo-0
  region: ap-southeast-1
  version: "1.30"

iam:
  withOIDC: true

addonsConfig:
  disableDefaultAddons: false
addons:
- name: vpc-cni
- name: kube-proxy
- name: coredns
